# In dev we use Vite's proxy (see vite.config.ts), so this can stay empty.
# For deployed builds (Phase 4), set it to your backend's public URL:
# VITE_API_BASE_URL=https://hrms-api.onrender.com
VITE_API_BASE_URL=
